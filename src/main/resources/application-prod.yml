spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mqtt_service?useSSL=false
    username: root
    password: CmileTally2db
    driverClassName: com.mysql.cj.jdbc.Driver

server:
  port: 9001
  error:
    include-message: always
